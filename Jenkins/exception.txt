<distributionManagement>
  <repository>
    <id>ArtifactoryEaiMavenRepository</id>
    <url>https://ARTIFACTORY_HOST/artifactory/TON_REPO_RELEASES</url>
  </repository>
  <snapshotRepository>
    <id>ArtifactoryEaiMavenRepository</id>
    <url>https://ARTIFACTORY_HOST/artifactory/TON_REPO_SNAPSHOTS</url>
  </snapshotRepository>
</distributionManagement>

mvn deploy \
  -DaltDeploymentRepository=ArtifactoryEaiMavenRepository::default::https://ARTIFACTORY_HOST/artifactory/TON_REPO

<servers>
	<server>
		<id>ArtifactoryEaiMavenRepository</id>		
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
	<server>
		<id>ArtifactoryGenericLocalSasRepository</id>		
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
	<server>
		<id>ArtifactoryMavenLocalSasRepository</id>		
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
	<server>
		<id>ArtifactoryMavenLocalSasPlugins</id>		
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
	<server>
		<id>ArtifactoryMavenCentralMirror</id>
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
	<server>
		<id>ArtifactoryJcenterBintrayPlugins</id>		
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
	<server>
		<id>ArtifactoryGradlePlugins</id>		
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
	<server>
		<id>ArtifactoryApachePlugins</id>		
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
	<server>
		<id>ArtifactoryOssPlugins</id>		
		<username>${eai.build.artifact.username}</username>
		<password>${eai.build.artifact.password}</password>
	</server>
  </servers>
