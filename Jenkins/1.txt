  out.tokenize(",")
           .collect { it.trim() }
           .findAll { !it.toUpperCase().contains("SNAPSHOT") } // Filtre SNAPSHOT
           .sort().reverse() // Plus récent en haut
        : []
