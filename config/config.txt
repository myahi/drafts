Lancer l'application:
java -jar app.jar --spring.config.additional-location=file:/etc/monap

  <!-- Actuator (nécessaire pour /actuator/...) -->
<dependency>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>

<!-- Jolokia 2 + auto-config Spring Boot 3 -->
<dependency>
  <groupId>org.jolokia</groupId>
  <artifactId>jolokia-support-spring</artifactId>
</dependency>


  spring:
  main:
    web-application-type: none
  application:
    name: mon-app-camel

management:
  server:
    port: 18081
  endpoints:
    web:
      exposure:
        include: jolokia
  endpoint:
    jolokia:
      enabled: true
