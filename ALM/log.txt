Path outputDirectory = Paths.get(outputDir);

String outputFileName = clientFunding.getFileName()
    .toString()
    .replace(".csv", "_OUT.csv");

Path transformed = outputDirectory.resolve(outputFileName);
