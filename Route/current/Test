import org.apache.camel.ServiceStatus;

ServiceStatus status = camelContext.getRouteController().getRouteStatus(routeId);
log.info("About to start routeId={}, currentStatus={}", routeId, status);


log.info("After start routeId={}, status={}", routeId,
         camelContext.getRouteController().getRouteStatus(routeId));
